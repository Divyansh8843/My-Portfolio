document.querySelectorAll(".pi").forEach(function (el) {
  new Typed(el, {
    strings: ["Web Developer", "Competetive Programmer", "Frontend Developer"],
    loop: true,
    typeSpeed: 100,
    backSpeed: 100,
    startDelay: 100,
    backDelay: 1500,
  });
});
